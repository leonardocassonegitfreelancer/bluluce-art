# Skip Puppeteer's Chromium download during install.
# Puppeteer is only used by the local screenshot helper (screenshot-jf.mjs),
# never by the static Astro build, so the deploy/CI install must not download
# ~150MB of Chromium on every run.
puppeteer_skip_download=true
